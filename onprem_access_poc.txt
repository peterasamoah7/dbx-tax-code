/**
 * 
1. Register a new App Registration in Entra ID
2. Generate a certificat for authentication and install 
this on the Windows machine or VM or local. I used local 
for testing and you can rename it to something else as well
as change the pat. Thats the Ps script below. Its create the 
script and print out the Thumbprint

//$cert = New-SelfSignedCertificate `
//    -Subject "CN=MyBlobWindowsService" `
//    -CertStoreLocation "Cert:\LocalMachine\My" `
//    -KeySpec Signature `
//    -KeyLength 2048 `
//    -KeyAlgorithm RSA `
//    -HashAlgorithm SHA256 `
//    -NotAfter (Get-Date).AddYears(1)

//$cert.Thumbprint

3. Export the certificate to your machine
//Export-Certificate `
//    -Cert $cert `
// -FilePath "C:\Temp\MyBlobWindowsService.cer"
 
4. Create a Storage Account in your resource group. 

5. Create a Blob storage. Give the app registration service
principal access to read or contribute to the storage account

6. Run this bit of code to get the current user. Once you 
get the user you can give the user access to read the certificate
on the machine. Ideally this will be setup in the deployment.
So this way only system accounts can read the cert. Solves the 
security issue of anyone being able to access the cert. 
//Console.WriteLine(WindowsIdentity.GetCurrent().Name);

7. This is a small application to test this process. 
//using Azure.Identity;
//using Azure.Storage.Blobs;
//using System.Security.Cryptography.X509Certificates;
//using System.Security.Principal;

//var tenantId = "";
//var clientId = "";
//var thumbprint = "";

//var storageUri = "";

//// 1. Find certificate in Windows certificate store
//using var store = new X509Store(
//    StoreName.My,
//    StoreLocation.LocalMachine);

//store.Open(OpenFlags.ReadOnly);

//var certificates = store.Certificates.Find(
//    X509FindType.FindByThumbprint,
//    thumbprint,
//    validOnly: false);

//if (certificates.Count == 0)
//{
//    throw new Exception("Certificate not found.");
//}

//var certificate = certificates[0];

//Console.WriteLine($"Certificate: {certificate.Subject}");
//Console.WriteLine($"Thumbprint: {certificate.Thumbprint}");
//Console.WriteLine($"Has private key: {certificate.HasPrivateKey}");
//Console.WriteLine($"Has private key: {certificate.HasPrivateKey}");

//try
//{
//    using var rsa = certificate.GetRSAPrivateKey();

//    Console.WriteLine(
//        rsa == null
//            ? "RSA private key is NULL"
//            : $"RSA private key accessible: {rsa.KeySize} bits");
//}
//catch (Exception ex)
//{
//    Console.WriteLine($"Private key FAILED: {ex}");
//}

//// 2. Authenticate as the Entra application
//var credential = new ClientCertificateCredential(
//    tenantId,
//    clientId,
//    certificate);

//// 3. Create Blob Storage client
//var blobServiceClient = new BlobServiceClient(
//    new Uri(storageUri),
//    credential);

//// 4. Test access
//Console.WriteLine();
//Console.WriteLine("Containers:");

//await foreach (var container in blobServiceClient.GetBlobContainersAsync())
//{
//    Console.WriteLine($" - {container.Name}");
//}

//Console.WriteLine();
//Console.WriteLine("Success!");
 
 
 */
